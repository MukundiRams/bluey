# Environment-specific values. Never put AWS secrets in this file.
# Resource identifiers are intentionally generated by CloudFormation/CDK.

dev:
  aws_profile: bluey-dev
  region: us-east-1
  stage: dev

hackathon:
  aws_profile: bluey-hackathon
  region: us-east-1
  stage: hackathon
